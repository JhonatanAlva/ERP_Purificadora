# Copiar a ".env" (ya está en .gitignore) y ajustar valores antes de levantar docker-compose.

POSTGRES_USER=puri_user
POSTGRES_PASSWORD=cambia-esta-contrasena
POSTGRES_DB=purificadora_db

PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=cambia-esta-contrasena
